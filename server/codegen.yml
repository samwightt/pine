overwrite: true
schema: "../schema.graphql"
documents: null
generates:
  types/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
