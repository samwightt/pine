import { gql } from 'apollo-server';
import createModule from 'utils/createModule';

export const {{uppercase name}}Module = createModule("{{uppercase name}}", {
  resolvers: {
    Query: {},
    {{uppercase name}}: {},
  },
  schema: gql`
    type {{uppercase name}} implements Node {
      id: ID!
    }
  `,
  nodeResolver(id) {
    return {};
  },
});